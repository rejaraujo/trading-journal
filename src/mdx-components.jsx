export function useMDXComponents(components) {
  return {
    ...components,
  };
}

// this is where we can map different components you want to be rendered for each HTML element or when you markdown has been parsed into HTML or JSX
// you can map what you want for each one.
